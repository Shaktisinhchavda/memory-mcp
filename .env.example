# ============================================
# Personal MCP Ecosystem — Environment Config
# ============================================
# Copy this file to .env and adjust values.

# --- Paths ---
# Base directory for all personal data
DATA_DIR=./data
NOTES_DIR=./data/notes
FILES_DIR=./data/files

# --- Vector Store ---
# ChromaDB persistent storage location
CHROMA_DB_PATH=./.chroma
# Embedding model (runs locally via sentence-transformers)
EMBEDDING_MODEL=all-MiniLM-L6-v2

# --- Event Logger ---
# SQLite database for file change events
EVENTS_DB_PATH=./events.db

# --- MCP Server ---
# Server name shown to MCP clients
MCP_SERVER_NAME=memory-mcp
# Transport: stdio (for Claude Desktop) or sse (for HTTP clients)
MCP_TRANSPORT=stdio
